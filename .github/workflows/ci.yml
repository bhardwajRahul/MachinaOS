# CI workflow - validates on every push and PR to main
# Delegates all validation to predeploy.yml

name: CI

on:
  push:
    branches: [main, pnpm-phased-migration]
  pull_request:
    branches: [main]

jobs:
  predeploy:
    uses: ./.github/workflows/predeploy.yml
